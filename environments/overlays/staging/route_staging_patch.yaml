apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: project-route
  labels:
    app: project-route
spec:
  parentRefs:
    - name: staging-project-gateway
  rules:
  - matches:
      - path:
          type: PathPrefix
          value: /
    backendRefs:
      - name: staging-project-svc
        port: 1234



